- name: "add group hosts to /etc/hosts"
  lineinfile:
    dest: /etc/hosts
    regexp: ".*{{ hostvars[item]['inventory_hostname'] }}(\\s*)$"
    line: "{{ hostvars[item]['ansible_'+iface]['ipv4']['address'] }}	{{ hostvars[item]['inventory_hostname'] }}"
  with_items: "{{ groups[group] }}"
